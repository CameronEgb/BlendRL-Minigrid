# @package _global_
defaults:
  - cew/_base
  - override /env: cartpole

total_timesteps: 250000
intervals_count: 10
eval_episodes: 100
online_methods: ppo/cp_tuned
offline_methods: cew/base,cew/fyd
offline_datasets: ppo/cp_tuned

agent:
  epochs_per_interval: 50

trainer:
  limit_train_batches: 500

save_dataset: true
seed: 1
local: false
recover: false
