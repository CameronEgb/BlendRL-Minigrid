agent:
  agent:
    name: iql
    algorithm: iql
    lr: 0.0003
    tau: 0.7
    beta: 3.0
    batch_size: 256
    epochs_per_interval: 10
    ent_coef: 0.0
    clip_coef: 0.2
  tau: 0.7025371810175959
  beta: 9.996599417828824
  lr: 0.00487084647758663
  batch_size: 256
  epochs_per_interval: 10
  ent_coef: 0.012854843414740148
  clip_coef: 0.2
  name: iql_cp_tuned
env:
  name: cartpole
  env_id: CartPole-v1
  num_envs: 4
  num_steps: 500
  gamma: 0.99
  gae_lambda: 0.95
  rules: default
  reasoner: nsfr
  architecture: mlp
mode:
  name: offline
  type: offline
  dataset_path: results/datasets/test/quick_test/ppo_cp_tuned
  save_dataset: false
experiment_id: quick_test
group: test
seed: 1
total_timesteps: 10000
intervals_count: 5
eval_episodes: 10
save_dataset: true
local: true
recover: false
results_dir: results
experiment_dir: ${results_dir}/experiments/${experiment_id}
dataset_root: ${results_dir}/datasets/${experiment_id}
dataset_path: ${dataset_root}/${agent.name}/${hydra:job.num}
online_methods: ppo/cp_tuned,blendrl/cp_tuned
offline_methods: iql/cp_tuned,blendrl_iql/cp_tuned
offline_datasets: ppo/cp_tuned
trainer:
  accelerator: cpu
