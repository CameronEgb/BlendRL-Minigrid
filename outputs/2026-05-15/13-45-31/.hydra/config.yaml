agent:
  agent:
    name: ppo
    algorithm: ppo
    lr: 0.0003
    ent_coef: 0.01
    vf_coef: 0.5
    max_grad_norm: 0.5
    clip_coef: 0.2
    update_epochs: 10
    num_minibatches: 4
    batch_size: 64
    anneal_lr: true
    norm_adv: true
    clip_vloss: true
    hidden_sizes:
    - 64
    - 64
  lr: 0.003
  ent_coef: 0.01
  num_envs: 10
  num_steps: 256
  num_minibatches: 4
  update_epochs: 10
  gamma: 0.99
  gae_lambda: 0.95
  clip_coef: 0.2
  max_grad_norm: 0.5
  name: ppo_cp_tuned
env:
  name: cartpole
  env_id: CartPole-v1
  num_envs: 4
  num_steps: 500
  gamma: 0.99
  gae_lambda: 0.95
  rules: default
  reasoner: nsfr
  architecture: mlp
mode:
  name: online
  type: online
  save_dataset: true
experiment_id: cp_final
seed: 1
total_timesteps: 1
intervals_count: 10
eval_episodes: 100
save_dataset: true
local: true
recover: false
results_dir: results
experiment_dir: ${results_dir}/experiments/${experiment_id}
dataset_root: ${results_dir}/datasets/${experiment_id}
dataset_path: results/datasets/cp_final/ppo_cp_tuned
online_methods: ppo/cp_tuned,blendrl/cp_tuned
offline_methods: iql/cp_tuned,blendrl_iql/cp_tuned
offline_datasets: ppo/cp_tuned,blendrl/cp_tuned
