# @package _global_
defaults:
  - mimic/_base

consolidate: true
env:
  reward_type: tqn

agent:
  epochs_per_interval: 30
  eval_interval_epochs: 5
  hidden_sizes: [512, 512]
  modules:
    - type: nsfr
      rules: simplified
    - type: neural
