# @package _global_
defaults:
  - override /env: cartpole

experiment_id: cew_full_run
group: cew
total_timesteps: 250000
intervals_count: 10
eval_episodes: 100
online_methods: ppo/cp_tuned
offline_methods: cew,cew_fyd
offline_datasets: ppo/cp_tuned

agent:
  epochs_per_interval: 50

trainer:
  limit_train_batches: 500

save_dataset: true
seed: 1
local: false
recover: false
