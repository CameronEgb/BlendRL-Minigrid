# @package _global_
defaults:
  - override /env: cartpole

experiment_id: blending
group: ungrouped
total_timesteps: 100000
eval_episodes: 100
online_methods: ppo/cp_tuned,blendrl/multi_logic
offline_methods: iql/cp_tuned,blendrl_iql/multi_logic
intervals_count: 10

env:
  rules: "default, variant1"
  architecture: mlp

save_dataset: true
seed: 1
local: true
recover: false
