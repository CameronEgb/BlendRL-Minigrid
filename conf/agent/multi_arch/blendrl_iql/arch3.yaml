defaults:
  - /agent: blendrl_iql_base
  - _self_

modules:
  - type: nsfr
    rules: default
  - type: cew
    fyd: true
blender_mode: neural
actor_mode: logic
fyd: true
ecm_dthr: 0.1
env:
  rules: default
