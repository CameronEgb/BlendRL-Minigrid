agent:
  agent:
    name: blendrl
    algorithm: blendrl
    lr: 0.00025
    logic_lr: 0.00025
    blender_lr: 0.00025
    ent_coef: 0.01
    blend_ent_coef: 0.01
    vf_coef: 0.5
    max_grad_norm: 0.5
    clip_coef: 0.1
    update_epochs: 4
    num_minibatches: 4
    anneal_lr: true
    batch_size: 64
    norm_adv: true
    clip_vloss: true
    blender_mode: logic
    blend_function: softmax
    actor_mode: hybrid
    joint_training: false
    pretrained: false
  lr: 0.009230439724281817
  logic_lr: 0.01
  blender_lr: 0.01
  ent_coef: 0.017957308902020765
  blend_ent_coef: 0.001
  vf_coef: 0.5
  max_grad_norm: 0.5
  clip_coef: 0.2
  update_epochs: 10
  num_minibatches: 4
  anneal_lr: true
  batch_size: 64
  norm_adv: true
  clip_vloss: true
  blender_mode: logic
  blend_function: softmax
  actor_mode: hybrid
  joint_training: true
  pretrained: false
  name: blendrl_cp_tuned
env:
  name: cartpole
  env_id: CartPole-v1
  num_envs: 4
  num_steps: 500
  gamma: 0.99
  gae_lambda: 0.95
  rules: default
  reasoner: nsfr
  architecture: mlp
mode:
  name: online
  type: online
  save_dataset: true
experiment_id: quick_test
group: test
seed: 1
total_timesteps: 10000
intervals_count: 5
eval_episodes: 10
save_dataset: true
local: true
recover: false
results_dir: results
experiment_dir: ${results_dir}/experiments/${experiment_id}
dataset_root: ${results_dir}/datasets/${experiment_id}
dataset_path: results/datasets/test/quick_test/blendrl_cp_tuned
online_methods: ppo/cp_tuned,blendrl/cp_tuned
offline_methods: iql/cp_tuned,blendrl_iql/cp_tuned
offline_datasets: ppo/cp_tuned
trainer:
  accelerator: cpu
