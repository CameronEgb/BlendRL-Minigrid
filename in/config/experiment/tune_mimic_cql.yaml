# @package _global_
defaults:
  - override /env: mimic
  - override /agent: cql_base
  - override /mode: offline
  - override /hydra/sweeper: optuna

experiment_id: "tune_mimic_cql"
group: tuning
total_timesteps: 99142
intervals_count: 5  # Sweep across 5 data-efficiency intervals (20%, 40%, 60%, 80%, 100%)
eval_episodes: 100

online_methods: []
offline_methods: [cql_base]
offline_datasets: [mimic]

agent:
  hidden_sizes: [256, 256]
  epochs_per_interval: 50
  eval_interval_epochs: 10
  batch_size: 64

mode:
  dataset_path: in/datasets/mimic/cql

hydra:
  sweeper:
    sampler:
      _target_: optuna.samplers.TPESampler
      seed: 123
    _target_: hydra_plugins.hydra_optuna_sweeper.optuna_sweeper.OptunaSweeper
    direction: maximize
    storage: "sqlite:///results/optuna/optuna_${experiment_id}.db"
    study_name: "tune_mimic_cql_${agent.name}"
    n_trials: 40
    n_jobs: 1
    params:
      agent.lr: interval(1e-4, 1e-3)
      agent.cql_alpha: interval(0.1, 5.0)
